
version: "3"

services:

  application:
    build: ./app
    container_name: JustShareIt_api
    environment: 
      PYTHONPATH: .
    ports:
      - '5000:5000'
    links:
      - redis
  
  redis:
    build: ./redis
    container_name: JustShareIt_redis
    ports:
      - '6379:6379'
