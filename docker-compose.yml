
version: "3.7"

services:

  application:
    build: ./app
    container_name: JustShareIt_api
    volumes: 
      - type: bind
        source: "//c/Users"
        target: "/mnt/c/Users"
        read_only: True
    environment: 
      PYTHONPATH: .
    ports:
      - '5000:5000'
    links:
      - redis
  
  redis:
    build: ./redis
    container_name: JustShareIt_redis
    ports:
      - '6379:6379'
